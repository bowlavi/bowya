#include <stdio.h>

void main()

{

    int p,q,num;
    printf("enter the p,q value");
    scanf("%d%d",&p,&q);
    {
        for(i=p;i<=q,i++)
        {
            if(i%2==1)
            printf("print the i value%d",i);
        }
    }

}
